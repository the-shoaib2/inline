{
  "name": "inline-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "compile": "pnpm --filter inline-extension run compile",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "typescript": "^5.1.3"
  }
}