{
  "name": "inline-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "compile": "pnpm --filter inline compile",
    "lint": "pnpm -r run lint",
    "reset": "./scripts/reset.sh",
    "dev": "pnpm run compile --watch"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}